# Validation using picrust1

Run scripts in order:

```
/usr/bin/R --vanilla create_folds.R
## Conda or picrust environment must be active with R installed and the ape pacakge
./picrust.sh
/usr/bin/R --vanilla validation.R
 ```
